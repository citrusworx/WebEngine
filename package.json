{
    "name": "citrusworx",
    "private": true,
    "packageManager": "yarn@4.13.0",
    "workspaces": [
        "apps/*",
        "apps/*/*",
        "engines/*",
        "libraries/*",
        "packages/*",
        "services/*",
        "tooling/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev --parallel"
    },
    "devDependencies": {
        "@playwright/test": "^1.58.2",
        "@types/node": "^25.4.0",
        "turbo": "^2.8.15",
        "typescript": "^5.9.3"
    }
}
