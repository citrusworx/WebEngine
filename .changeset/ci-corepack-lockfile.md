---
---

CI-only: enable Corepack before setup-node, sync yarn.lock workspace metadata, and raise the Juice core CSS artifact size budget to match the already-shipped 8.18MB dist. No package release.
