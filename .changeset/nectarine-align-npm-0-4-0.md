---
"@citrusworx/nectarine": patch
---

Record published npm 0.4.0 in the workspace manifest. That publish did not commit the version bump. No new compiler surface. Pending ON CONFLICT changesets still make the next intentional publish 0.5.0.
