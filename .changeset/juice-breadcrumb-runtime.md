---
"@citrusworx/juiceui": minor
---

Add a light DOM-first breadcrumb runtime (`createBreadcrumb` / `sync` / `setCurrent`) that names an unlabeled navigation landmark and keeps a single `aria-current="page"`.
