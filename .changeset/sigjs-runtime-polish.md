---
"@citrusworx/sigjs": patch
---

Make `batch()` nested-safe and exception-safe, normalize `SigRouter.navigate` paths, add an optional `*` unknown-route fallback, exclude tests from published dist, and align Sig.js docs with the runtime.
