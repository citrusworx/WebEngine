---
"@citrusworx/sugar": minor
---

Harden the Sugar graph model so ports have unique per-node ids, edges reference specific ports instead of node categories, and rendering/hit-testing can place multiple input and output ports.
