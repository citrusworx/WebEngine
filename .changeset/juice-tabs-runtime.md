---
"@citrusworx/juiceui": minor
---

Add a DOM-first tabs runtime that auto-enhances valid `[tabs]` markup, with APG keyboard, exclusive panels, dual-write `[active]`/`aria-selected`, and shared `--juice-tabs-*` chrome roles.
