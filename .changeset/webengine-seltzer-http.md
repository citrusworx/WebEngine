---
"@citrusworx/webengine": minor
---

Add opt-in `startSeltzerFromKernel` / `serveNectarineHttp` after Nectarine bootstrap: `Seltzer.init()`, `handle.createRoutes`, `listen`. Kernel bootstrap still does not auto-listen.
