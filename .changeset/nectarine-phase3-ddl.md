---
"@citrusworx/nectarine": minor
---

Compile CREATE TABLE / indexes from `*Schema.yml` (Postgres JSONB first-class). Blackwater migrate() runs named compiled DDL; hand-written phase3-ddl is retired.
