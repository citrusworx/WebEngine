---
"@citrusworx/juiceui": minor
---

Add a DOM-first APG progress runtime (`createProgress` / `setValue` / `setIndeterminate`) that keeps `role="progressbar"`, value ARIA, `--juice-progress-ratio`, and `progress="indeterminate"` in sync.
