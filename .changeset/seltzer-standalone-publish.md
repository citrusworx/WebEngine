---
"@citrusworx/seltzer": patch
---

Publish/DX hygiene for standalone consumers: npm description, Node 18 engines, optional `undici` peer for `allowSelfSigned`, and a README that leads with `init().route().listen()`.
