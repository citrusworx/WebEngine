---
"@citrusworx/webengine": minor
---

Generate Seltzer write routes from Nectarine `*API.yml` (POST/PUT/PATCH/DELETE) via `createNectarineWriteRoutes` / `createNectarineRoutes`, with compiled execute and an `execute` escape hatch for JSONB and waitlist join.
