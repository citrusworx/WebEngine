---
"@citrusworx/kiwipress": minor
---

Add a generic WordPress taxonomy client (`CustomTaxonomy` / `kiwi.wordpress.taxonomy(restBase)`), taxonomy discovery at `wordpress.taxonomies`, and opt-in WPSync transfer via `taxonomies`.
