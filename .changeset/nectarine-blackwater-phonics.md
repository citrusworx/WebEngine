---
"@citrusworx/nectarine": minor
---

Compile Blackwater `type: SELECT` query YAML (and `read` as `get`) onto the phonics compiler. Closed where/orderBy fragment grammar; no hard-coded SQL in app code.
