---
"@citrusworx/grapevine": minor
---

Add an applied `stack` section (compose/env/bootstrap → droplet cloud-init) and `resources.databases` for DigitalOcean managed databases, plus executable `kiwipress-compose` and `kiwipress-managed` blueprint packs.
