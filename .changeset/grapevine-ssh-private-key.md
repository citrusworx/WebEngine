---
"@citrusworx/grapevine": minor
---

Persist generated SSH private keys on `grape apply` (`generate: true`) to `private_key_path` or `.grape/ssh/<name>`, report the path in the apply result, and never include key material in JSON output.
