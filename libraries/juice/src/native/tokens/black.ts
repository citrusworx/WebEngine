import { StyleSheet } from 'react-native';
import black from '../../tokens/color/black/black.json';

export const bg = StyleSheet.create({
    screen: {
        backgroundColor: black.black[200],
        padding: 10
    }
})