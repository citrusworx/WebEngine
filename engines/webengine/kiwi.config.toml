version = "0.1.0"

[kernel]
modules = ["core", "web"]

[runtimes.web]
path = "webengine.config.json5"

[webengine]
app_name = "webengine-sample"
description = "Sample WebEngine project config for local smoke and docs"
host = "localhost"
port = 8080
debug_mode = true
log_level = "info"
